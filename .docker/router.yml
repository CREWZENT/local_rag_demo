http:
  routers:
    ollama:
      rule: "PathPrefix(`/api`)"
      service: ollama-gpu
      entryPoints:
        - web

  services:
    ollama-gpu:
      loadBalancer:
        servers:
          - url: "http://ollama-gpu:11434" 